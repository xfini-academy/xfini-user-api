$kind: http-example
request:
  url: '{{baseUrl}}/expiring-students?date=invalid-date'
  method: GET
  queryParams:
    date: invalid-date
response:
  statusCode: 400
  statusText: Bad Request
  headers:
    Content-Type: application/json; charset=utf-8
  body:
    type: json
    content: |-
      {
        "success": false,
        "code": "INVALID_INPUT",
        "error": "Invalid date format. Expected YYYY-MM-DD."
      }
order: 2000
